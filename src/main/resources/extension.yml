name: ${name}
version: ${version}

mains:
  papermc: "${group}.papermc.ServerTools"
  spigotmc: "${group}.spigotmc.ServerTools"
  foliamc: "${group}.foliamc.ServerTools"

git:
  provider: github
  owner: MCPaimon-Extension
  repository: server
